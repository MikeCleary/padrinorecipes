<div class="comments_form">
  <% form_for @comment, url_for(:comments, :create,) do |f| %>
    <%= f.hidden_field :recipe_id %>
    <%= f.text_area :content, :cols => 60, :rows => 6, :placeholder => "Your Comment..." %></br>
    <%= f.submit :value => "Post comment" %>
  <% end %>
</div>